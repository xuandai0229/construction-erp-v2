# GLOBAL CONTEXT NOTIFICATION FINANCE PRE FIX ANALYSIS

## Bảng phân tích

| Khu vực | Lỗi UAT | Bằng chứng từ ảnh | Nguyên nhân nghi ngờ | File cần kiểm tra | Cách sửa | Tiêu chí nghiệm thu |
| --- | --- | --- | --- | --- | --- | --- |
| **Project Context** | Topbar chọn dự án A, nhưng Hợp đồng/Thanh toán hiển thị dự án B. | Topbar hiện "Dự án Trần Quang Hiếu", Hợp đồng hiện "HN-TH-2026-001". | Module Hợp đồng và Thanh toán chưa dùng `getGlobalProjectContext` để đồng bộ URL/Cookie, mà tự parse hoặc lấy dự án đầu tiên/toàn bộ. | `contracts/page.tsx`, `contracts-workspace.tsx`, `accounting/page.tsx`, `accounting-workspace.tsx` | Dùng `getGlobalProjectContext` truyền vào các page. Đảm bảo UI update cookie khi có thay đổi local. | Khi chọn topbar, bảng Hợp đồng/Thanh toán lọc đúng. |
| **Notification** | Click thông báo gây crash màn Reports | Crash `PrismaClientValidationError` tại `prisma.siteReport.count()` | Truyền `status=ISSUE` hoặc id không hợp lệ (không phải UUID/Cuid) vào prisma nơi đòi hỏi Enum hợp lệ, khiến Prisma báo lỗi validation. | `reports/actions.ts`, `reports/page.tsx` | Kiểm tra tính hợp lệ của query param trước khi ném vào prisma (vd: kiểm tra `status` có thuộc Enum của SiteReport không, hoặc catch error an toàn). | Bấm notification đi đến đúng báo cáo mà không crash. |
| **Search Topbar** | Search chỉ hiện placeholder/đang phát triển | Chưa hoạt động | Chưa implement search thật vào component | `global-search-command.tsx` | Nâng cấp component đã tạo để đảm bảo filter chính xác theo keyword, có thể click đến đúng link. | Search hoạt động và mở popup command. |
| **Help Icon** | Nội dung chưa hữu ích/vẫn còn nguyên bản nháp | Nội dung không giống yêu cầu | Text trong component chưa được update chuẩn theo spec mới | `header.tsx` | Cập nhật chính xác 5 dòng hướng dẫn nghiệp vụ vào popover. | Help hiển thị đúng text, mở bằng click. |
| **Dashboard Chips** | Các chip KPI công việc/rủi ro 0 không click được | Lỗi onClick server component | Đã thay bằng thẻ `div` thay vì `Link` đối với trạng thái 0 | `executive-header.tsx` | Chuyển toàn bộ về `Link` và không dùng event handler để đi tới empty state. | Mọi chip đều click được (dùng Link). |
| **Finance KPI** | Nhầm lẫn dữ liệu rỗng và lỗi UI | Ảnh báo "Chưa có hợp đồng" | Dự án Trần Quang Hiếu thực sự rỗng data DB. Data hiển thị là của HN-TH-2026-001 | `scripts/audit-finance-context-consistency.js` | Viết script chứng minh rạch ròi dữ liệu của từng dự án. | Report in ra rõ ràng sự tách biệt data. |
