# Phân tích UX của Search và Report Drawer (Pre-fix)

| Khu vực | Lỗi UAT | Nguyên nhân nghi ngờ | File cần kiểm tra | Cách sửa | Tiêu chí nghiệm thu |
| --- | --- | --- | --- | --- | --- |
| **Search Command Palette** | Căn lề giữa màn hình quá nhiều khoảng trống, click outside không đóng. | Layout đang dùng `pt-[10vh]` để căn giữa, overlay thiếu logic click outside, chưa bắt sự kiện mousedown ở overlay. | `src/components/layout/global-search-command.tsx` | Đổi sang `fixed top-[72px] inset-x-0 mx-auto`, xoá `pt-[10vh]`. Thêm `useRef` và listener mousedown trên document để đóng. Giữ ESC event. | Popup search nằm sát topbar (cách một chút), click ngoài tự đóng, ESC đóng. Giao diện gọn hơn. |
| **Report Detail Drawer** | Đóng khựng, giật, lag. Không thể tắt bằng click outside hoặc ESC. | Đóng drawer trigger set lại URL xoá `reportId` bằng push. Có thể đang re-render toàn bộ list khi tắt hoặc thiếu animation transition trên detail drawer. Click outside/ESC chưa được config trong thành phần DetailDrawer. | `src/components/reports/reports-workspace.tsx`, `src/components/reports/report-detail-drawer.tsx` | Xóa `reportId` trong URL nhẹ nhàng bằng `router.replace(..., { scroll: false })` hoặc update URL tĩnh. Thêm click outside cho drawer. Chỉnh CSS transition opacity/translate. | Bấm nút X, nút Đóng, ESC, click outside đều đóng mượt, không giật màn hình, không fetch lại list không cần thiết. |
