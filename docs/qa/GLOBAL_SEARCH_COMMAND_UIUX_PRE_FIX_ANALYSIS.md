# GLOBAL SEARCH COMMAND UIUX PRE-FIX ANALYSIS

| Khu vực | Lỗi UAT | Nguyên nhân | File cần sửa | Hướng sửa | Tiêu chí nghiệm thu |
|---|---|---|---|---|---|
| **Vị trí & Layout** | Popup căn lề ở giữa màn hình, khoảng trắng thừa nhiều, giống Modal thay vì Command Palette. | Layout cũ vẫn dùng style modal hoặc cách top quá nhiều. | `src/components/layout/global-search-command.tsx` | Chỉnh container thành `fixed inset-x-0 top-[76px] mx-auto z-50`. `max-w-3xl` hoặc `max-w-4xl`. Bỏ phần padding top dạng `vh`. | Gần topbar, không làm tốn diện tích vô nghĩa. |
| **Overlay** | Nền bị mờ/tối hơi nặng. | Backdrop-blur hoặc bg-opacity cao. | `src/components/layout/global-search-command.tsx` | Đổi bg overlay thành `bg-slate-900/10`, bỏ backdrop-blur. | Nền tối nhẹ nhàng, vẫn nhìn thấy content dưới. |
| **Header/Input** | Input thiếu UX, chỉ có nút X đơn điệu, placeholder chưa hấp dẫn. | Header design cũ. | `src/components/layout/global-search-command.tsx` | Style lại input (ring, border), thêm biểu tượng ESC. Placeholder: "Tìm công trình, báo cáo, hồ sơ, thông báo..." | Input đẹp, có focus ring xanh nhạt. Có nút Esc tắt. |
| **Empty State (Chưa có keyword)** | Quá trống, chỉ có 2 dòng Gợi ý và Cần chú ý. Chưa có cấu trúc chia 2 nhóm "Tìm nhanh" và "Cần chú ý". | Layout chưa chia lưới, nội dung chưa mô phỏng Command menu. | `src/components/layout/global-search-command.tsx` | Chia 2 nhóm. Bên trái: Quick links (Công trình, Báo cáo, etc.). Bên phải: Notifications. | Giao diện Command Palette chuẩn, không khoảng trống vô duyên. |
| **Result Items** | List item kết quả chưa đủ "cao cấp", hover/focus state chưa tối ưu. | Padding/margin và icon style của result item còn cơ bản. | `src/components/layout/global-search-command.tsx` | Tối ưu item: Thêm subtitle nhỏ, icon màu tinh tế, hover `bg-slate-50`. | Hover mượt, nhìn chuyên nghiệp như Spotlight/Raycast. |
| **Footer** | Hiển thị "Đang sử dụng hệ thống tìm kiếm" nhìn không chuyên nghiệp. | Text hardcode từ phiên bản đầu. | `src/components/layout/global-search-command.tsx` | Đổi footer hiển thị phím tắt: mũi tên lên/xuống, Enter, ESC, Ctrl+K. | Footer tinh gọn, chỉ chứa keyboard hints. |
| **Keyboard UX** | Arrow Up/Down chưa hỗ trợ trọn vẹn, hoặc Esc/CmdK cần củng cố. | Chưa viết logic arrow key navigation. | `src/components/layout/global-search-command.tsx` | Bổ sung logic (nếu có thể) cho phím điều hướng hoặc ít nhất đảm bảo auto-focus, Esc và Click outside hoạt động chuẩn nhất. | Hỗ trợ bàn phím tối thiểu (Esc đóng, gõ ngay). |
