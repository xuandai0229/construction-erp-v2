# Phân tích lỗi Search, Notification, Finance KPI (Pre-fix)

| Khu vực | Lỗi UAT | Bằng chứng từ ảnh | Nguyên nhân nghi ngờ | File cần kiểm tra | Cách sửa | Tiêu chí nghiệm thu |
| --- | --- | --- | --- | --- | --- | --- |
| **Search Topbar** | Popup rỗng, chữ "Giai đoạn 1", overlay quá đậm, chưa hiển thị kết quả thật | Người dùng báo UI xấu, popup rỗng và che dashboard | Chưa implement thiết kế command palette chuẩn. Đang dùng backdrop-blur-sm với bg-slate-900/40 quá tối. | `src/components/layout/global-search-command.tsx`, `src/app/actions/global-search.ts` | Giảm độ đậm overlay, thiết kế lại empty state hữu ích, cập nhật UI command palette chuẩn, xoá chữ "Giai đoạn 1" | UI Command palette hiển thị gợi ý khi rỗng, có loading, tìm đúng project, report, notification, không còn "Giai đoạn 1" |
| **Notification** | Lệch ngày báo cáo và ngày thông báo, click không đóng popover | Báo cáo ngày 26/06 nhưng notification báo 29/06. Popover đè lên màn Reports | Notification lấy `createdAt` làm ngày duy nhất mà không ghi chú rõ đây là ngày thông báo. Thiếu sự kiện đóng popover khi click `Link`. | `src/lib/project-context.ts`, `src/components/layout/global-notification-bell.tsx` | Phân biệt rõ `ngày báo cáo` và `ngày thông báo` trong notification title/message. Thêm hàm đóng popover onClick | Thông báo ghi rõ "Báo cáo ngày X" và "Cập nhật ngày Y", popover tự đóng khi click |
| **Finance KPI** | Data trống (Trần Quang Hiếu) hoặc nhảy sai data (Tây Hồ) ở module con | KPI hiện `-` thay vì chú thích rõ. Màn Hợp đồng/Thanh toán không ăn theo Topbar | Dự án thực sự không có data nhưng UI báo trống gây lo lắng. Các module con chưa đọc `initialProjectId` từ context cookie. | `src/app/(dashboard)/dashboard/page.tsx`, `src/app/(dashboard)/contracts/page.tsx`, `src/app/(dashboard)/accounting/page.tsx`, `scripts/audit-finance-context-consistency.js` | Viết rõ "Chưa có hợp đồng trong công trình này". Đồng bộ URL param `projectId` với Workspace | Audit script xác nhận data. Bảng dashboard ghi rõ "Chưa có hợp đồng trong công trình này". Module Hợp đồng/Thanh toán filter theo Topbar |
