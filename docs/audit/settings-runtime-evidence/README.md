# Settings runtime evidence

Captured on 2026-08-03 against local QA in development Webpack and a fresh production build.

- `admin-*.png`: company/document/audit, dirty, validation, save and stale-update states.
- `director-*`, `deputy-*`, `staff-*`: RBAC evidence.
- `admin-laptop-*`, `admin-tablet-*`, `admin-mobile-*`: responsive evidence.
- `admin-production-settings.png`: authenticated production-build smoke test.
- `server-*.log`: local runtime diagnostics; no secret values are retained.

Temporary QA accounts and temporary configuration values used for runtime save tests were removed/restored after capture. Audit records are intentionally retained.
