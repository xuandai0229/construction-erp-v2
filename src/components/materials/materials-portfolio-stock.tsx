"use client";

import { useEffect, useMemo, useState } from "react";
import { ChevronDown, Search } from "lucide-react";
import { useRouter, useSearchParams } from "next/navigation";
import type { PortfolioStockItemDto } from "@/app/(dashboard)/materials/actions";
import { ContentCard, SafeText } from "@/components/ui/enterprise";
import { StockStatusBadge } from "./materials-badges";
import { formatDateTime, formatManufacturerOrigin, formatQuantity } from "./materials-formatters";

interface Props { stocks: PortfolioStockItemDto[]; onSelectProject: (id: string) => void; }

export function MaterialsPortfolioStock({ stocks, onSelectProject }: Props) {
  const router = useRouter(); const searchParams = useSearchParams(); const urlQuery = searchParams.get("q") || ""; const [query, setQuery] = useState(urlQuery);
  const view = searchParams.get("stockView") === "material" ? "material" : "project"; const expanded = searchParams.get("stockMaterial") || "";
  const update = (values: Record<string, string>) => { const params = new URLSearchParams(searchParams); Object.entries(values).forEach(([key, value]) => value ? params.set(key, value) : params.delete(key)); params.set("tab", "stock"); params.set("scope", "portfolio"); router.replace(`?${params.toString()}`, { scroll: false }); };
  useEffect(() => { setQuery((current) => current === urlQuery ? current : urlQuery); }, [urlQuery]);
  useEffect(() => { if (query === urlQuery) return; const timer = window.setTimeout(() => update({ q: query }), 250); return () => window.clearTimeout(timer); }, [query, urlQuery]);
  const filtered = useMemo(() => { const term = query.trim().toLocaleLowerCase("vi-VN"); return !term ? stocks : stocks.filter((stock) => [stock.name, stock.manufacturer, stock.origin, stock.unit, stock.code].filter(Boolean).some((value) => String(value).toLocaleLowerCase("vi-VN").includes(term))); }, [stocks, query]);
  const projects = useMemo(() => { const grouped = new Map<string, { id: string; name: string; entries: Array<{ item: PortfolioStockItemDto; stock: PortfolioStockItemDto["projectsBreakdown"][number] }> }>(); filtered.forEach((item) => item.projectsBreakdown.forEach((stock) => { const project = grouped.get(stock.projectId) || { id: stock.projectId, name: stock.projectName, entries: [] }; project.entries.push({ item, stock }); grouped.set(stock.projectId, project); })); return [...grouped.values()].sort((a, b) => a.name.localeCompare(b.name, "vi")); }, [filtered]);
  return <div className="space-y-4"><div className="flex flex-col gap-3 rounded-xl border border-[var(--border)] bg-[var(--surface)] p-3 shadow-[var(--shadow-card)] sm:flex-row sm:items-center"><label className="relative min-w-0 flex-1"><span className="sr-only">Tìm tồn kho vật tư</span><Search className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-[var(--muted-foreground)]" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Tìm theo mã, tên, hãng sản xuất hoặc xuất xứ..." className="h-10 w-full rounded-lg border border-[var(--border)] bg-[var(--surface)] pl-9 pr-3 text-sm outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100" /></label><div className="inline-flex h-10 shrink-0 rounded-lg border border-[var(--border)] bg-[var(--surface-subtle)] p-1"><button type="button" onClick={() => update({ stockView: "project" })} className={`rounded-md px-3 text-sm font-semibold ${view === "project" ? "bg-white text-slate-950 shadow-sm" : "text-slate-600"}`}>Theo công trình</button><button type="button" onClick={() => update({ stockView: "material" })} className={`rounded-md px-3 text-sm font-semibold ${view === "material" ? "bg-white text-slate-950 shadow-sm" : "text-slate-600"}`}>Theo vật tư</button></div></div>{view === "project" ? <div className="space-y-4">{projects.map((project) => { const low = project.entries.filter(({ stock }) => stock.stock < 0 || (stock.minStockLevel > 0 && stock.stock <= stock.minStockLevel)).length; const sufficient = project.entries.length - low; const latest = project.entries.map(({ stock }) => stock.lastUpdated).sort().at(-1); return <ContentCard key={project.id} className="overflow-hidden"><button type="button" onClick={() => onSelectProject(project.id)} className="flex w-full items-center justify-between gap-3 border-b border-[var(--border)] px-4 py-4 text-left transition hover:bg-slate-50 sm:px-5"><SafeText className="line-clamp-2 text-base font-bold leading-5 text-slate-950">{project.name}</SafeText><span className="shrink-0 text-sm text-[var(--muted-foreground)]">{project.entries.length} loại vật tư</span></button><div className="grid gap-3 border-b border-[var(--border)] bg-[var(--surface-subtle)] px-4 py-3 text-sm sm:grid-cols-4 sm:px-5"><div><p className="text-xs text-[var(--muted-foreground)]">Số loại vật tư</p><p className="mt-1 font-semibold tabular-nums">{project.entries.length}</p></div><div><p className="text-xs text-[var(--muted-foreground)]">Thiếu tồn</p><p className="mt-1 font-semibold tabular-nums text-amber-700">{low}</p></div><div><p className="text-xs text-[var(--muted-foreground)]">Đủ tồn</p><p className="mt-1 font-semibold tabular-nums text-emerald-700">{sufficient}</p></div><div><p className="text-xs text-[var(--muted-foreground)]">Hoạt động gần nhất</p><p className="mt-1 text-xs font-medium">{latest ? formatDateTime(latest) : "Chưa có"}</p></div></div><div className="divide-y divide-slate-100">{project.entries.map(({ item, stock }) => <div key={`${project.id}-${item.identity}`} className="grid gap-1 px-4 py-3 text-sm sm:grid-cols-[minmax(220px,1fr)_150px_120px] sm:items-center sm:px-5"><SafeText className="font-semibold text-slate-900">{item.name}</SafeText><span className="font-mono font-bold tabular-nums sm:text-right">{formatQuantity(stock.stock)} <span className="font-sans text-xs font-medium text-[var(--muted-foreground)]">{item.unit}</span></span><span className="sm:text-right"><StockStatusBadge stock={stock.stock} minStockLevel={stock.minStockLevel} compact /></span></div>)}</div></ContentCard>; })}{projects.length === 0 && <ContentCard className="px-5 py-10 text-center text-sm text-[var(--muted-foreground)]">Không tìm thấy dữ liệu tồn kho phù hợp.</ContentCard>}</div> : <ContentCard className="overflow-hidden"><div className="divide-y divide-slate-100">{filtered.map((item) => { const isOpen = expanded === item.identity; return <div key={item.identity}><button type="button" onClick={() => update({ stockMaterial: isOpen ? "" : item.identity })} className="grid w-full gap-2 px-4 py-4 text-left transition hover:bg-slate-50 sm:grid-cols-[minmax(240px,1fr)_130px_160px_130px] sm:items-center sm:px-5"><div className="flex min-w-0 items-center gap-2"><ChevronDown className={`h-4 w-4 shrink-0 text-slate-400 transition ${isOpen ? "rotate-180" : ""}`} /><div><SafeText className="font-semibold text-slate-900">{item.name}</SafeText><p className="mt-0.5 text-xs text-[var(--muted-foreground)]">{item.unit} · {formatManufacturerOrigin(item.manufacturer, item.origin, "Chưa cập nhật")}</p></div></div><span className="text-sm text-[var(--muted-foreground)]">{item.projectCount} công trình</span><span className="font-mono font-bold tabular-nums sm:text-right">{formatQuantity(item.totalStock)} <span className="font-sans text-xs font-medium text-[var(--muted-foreground)]">{item.unit}</span></span><span className="sm:text-right"><StockStatusBadge stock={item.totalStock} minStockLevel={0} compact /></span></button>{isOpen && <div className="border-t border-[var(--border)] bg-[var(--surface-subtle)] px-4 py-3 sm:px-5"><p className="mb-2 text-xs font-semibold uppercase tracking-wide text-[var(--muted-foreground)]">Tồn kho theo công trình</p><div className="divide-y divide-slate-200 rounded-lg border border-slate-200 bg-white">{item.projectsBreakdown.map((project) => <button type="button" key={project.projectId} onClick={() => onSelectProject(project.projectId)} className="flex w-full items-center justify-between gap-3 px-3 py-3 text-left hover:bg-slate-50"><SafeText className="line-clamp-2 font-medium text-slate-900">{project.projectName}</SafeText><div className="flex shrink-0 items-center gap-3"><span className="font-mono text-sm font-bold">{formatQuantity(project.stock)} {item.unit}</span><StockStatusBadge stock={project.stock} minStockLevel={project.minStockLevel} compact /></div></button>)}</div></div>}</div>; })}</div></ContentCard>}</div>;
}
