import { PrismaClient } from '@prisma/client'; import { PrismaPg } from '@prisma/adapter-pg'; import { Pool } from 'pg'; import bcrypt from 'bcryptjs'; const pool = new Pool({ connectionString: process.env.DATABASE_URL }); const adapter = new PrismaPg(pool); const prisma = new PrismaClient({ adapter }); async function main() { const hash = await bcrypt.hash('Admin@123', 10); await prisma.user.upsert({ where: { email: 'tayho.admin@seed.local' }, update: { password: hash, role: 'ADMIN', name: 'Admin', isActive: true, username: 'admin' }, create: { email: 'tayho.admin@seed.local', password: hash, name: 'Admin', role: 'ADMIN', isActive: true, username: 'admin' } }); console.log('User inserted'); process.exit(0); } main();