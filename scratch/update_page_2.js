const fs = require("fs"); let code = fs.readFileSync("src/app/(dashboard)/reports/page.tsx", "utf8"); code = code.replace("import { getVietnamDateString, getVietnamTimeString } from \\"@/lib/reports/report-timezone\\";", "import { getVietnamDateString, getVietnamTimeString } from \\"@/lib/reports/report-timezone\\";\\nimport { parseWeeklyGeneralNote } from \\"@/lib/reports/weekly-report-utils\\";"); const replaceBlock = "    nextWeekStartDate: (() => {\\n      try {\\n        if (r.generalNote) return JSON.parse(r.generalNote as string).nextWeekStartDate;\\n      } catch {}\\n      return undefined;\\n    })(),\\n    nextWeekEndDate: (() => {\\n      try {\\n        if (r.generalNote) return JSON.parse(r.generalNote as string).nextWeekEndDate;\\n      } catch {}\\n      return undefined;\\n    })(),\\n    nextWeekPlans: (() => {\\n      try {\\n        if (r.generalNote) return JSON.parse(r.generalNote as string).nextWeekPlans || [];\\n      } catch {}\\n      return [];\\n    })(),"; code = code.replace(replaceBlock, "    weeklyNote: parseWeeklyGeneralNote(r.generalNote as string),"); fs.writeFileSync("src/app/(dashboard)/reports/page.tsx", code);
